<Controls:MetroWindow x:Class="MailMerge.DoTemplateWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:Controls="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
        Title="DoTemplateWindow" Height="300" Width="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="40" />
            <RowDefinition Height="40" />
        </Grid.RowDefinitions>
        <DataGrid Grid.Row="0" x:Name="MergeFieldDataGrid"
                AutoGenerateColumns="False" />
        <TextBox x:Name="SubjectTextBox" Grid.Row="1" />
        <Button FontSize="10" Margin="10,0,188,10" VerticalAlignment="Bottom" Height="10" Click="AddRowButton_Click" Grid.Row="2">Add row</Button>
        <Button FontSize="10" Margin="188,0,10,10" VerticalAlignment="Bottom" Height="10" Click="MergeButton_Click" Grid.Row="2">Merge mails</Button>
    </Grid>
</Controls:MetroWindow>
